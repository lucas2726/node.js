const nome = "Lucas"

const soma = (a, b) => a + b

const subtracao = (a, b) => a - b

const divisao = (a, b) => a / b

const multiplicacao = (a, b) => a * b

module.exports = {
    nome,
    soma,
    subtracao,
    divisao,
    multiplicacao
}